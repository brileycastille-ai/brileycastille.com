import { getSupabaseAdmin } from "../../../lib/supabase-server";
export async function POST(request:Request){try{const payload=await request.json() as {question?:string;context?:string};const question=payload.question?.trim()??"";if(question.length<8||question.length>500)return Response.json({error:"Question must be 8 to 500 characters."},{status:400});const{error}=await getSupabaseAdmin().from("questions").insert({question,context:payload.context?.slice(0,160)||"standalone"});if(error)throw error;return Response.json({ok:true},{status:201})}catch{return Response.json({error:"Question service is not configured yet."},{status:503})}}
