import mammoth from "mammoth";import{getSupabaseAdmin}from"../../../lib/supabase-server";
const CREATOR="brileycastille@gmail.com";
export async function POST(request:Request){try{const token=request.headers.get("authorization")?.replace("Bearer ","");if(!token)return Response.json({error:"Sign in required"},{status:401});const sb=getSupabaseAdmin();const{data}=await sb.auth.getUser(token);if(data.user?.email?.toLowerCase()!==CREATOR)return Response.json({error:"Creator access only"},{status:403});const form=await request.formData();const file=form.get("file");const title=String(form.get("title")||"").trim();if(!(file instanceof File)||!title)return Response.json({error:"Title and Word document required"},{status:400});const buffer=Buffer.from(await file.arrayBuffer());const{value}=await mammoth.convertToHtml({buffer});const slug=title.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"");const{error}=await sb.from("essays").upsert({slug,title,content_html:value,status:"published",created_by:data.user.id},{onConflict:"slug"});if(error)throw error;return Response.json({ok:true,slug})}catch{return Response.json({error:"Publishing is not configured yet."},{status:503})}}
