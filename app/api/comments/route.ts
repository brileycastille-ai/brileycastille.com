import {getSupabaseAdmin}from"../../../lib/supabase-server";
export async function POST(request:Request){try{const p=await request.json()as{essay_slug?:string;paragraph_id?:string;body?:string;display_name?:string;email?:string};if(!p.body?.trim()||p.body.length>1500)return Response.json({error:"Comment is required."},{status:400});const{error}=await getSupabaseAdmin().from("comments").insert({essay_slug:p.essay_slug,paragraph_id:p.paragraph_id||null,body:p.body.trim(),display_name:p.display_name?.trim()||"Anonymous",guest_email:p.email?.trim()||null});if(error)throw error;return Response.json({ok:true},{status:201})}catch{return Response.json({error:"Comments are not connected yet."},{status:503})}}
